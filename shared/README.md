# Shared

Tool-agnostic prompt material that gets copied into individual project repos rather than
into a global config directory — `CLAUDE.md` templates, reusable prompt snippets, and
conventions I want a fresh repo to start with.

Empty for now.
